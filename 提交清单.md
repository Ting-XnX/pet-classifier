# 我的考核提交清单

姓名：王诗雯  
学号：W125301282

- 我的技术报告：`report/【考核】王诗雯_W125301282_宠物分类.pdf`，共 3 页。
- 我的完整工程：数据模块、模型模块、训练入口、独立评估、Grad-CAM、报告生成及一键运行脚本。
- 我的两组实验：Baseline 与 Label Smoothing 0.1，各训练 10 轮。
- 我的测试结果：Top-1 分别为 90.21% 和 92.11%；Macro-F1 分别为 0.9006 和 0.9204。
- 我的训练日志：`runs/baseline/tensorboard/` 和 `runs/label_smoothing/tensorboard/`，每组均包含 10 轮记录。
- 我的模型权重：两组目录下的 `best_model.pth`，分别选自第 2、9 轮。
- 我的可视化：训练曲线、完整 37 类混淆矩阵、局部矩阵、预测正确与错误的 Grad-CAM。
- 我的复盘与答辩：报告第 4 节、`runtime_verification.json` 与 `答辩准备.md`。
- 我的复核记录：`verification.json` 与 `final_verification.json`。

## 仓库发布状态

我已将完整代码、三页技术报告、实验图表、两组训练日志与评估结果上传至 https://github.com/Ting-XnX/pet-classifier ，可直接提交该仓库链接。

完整交付包保留训练权重和本地实验 Git 历史；远程仓库不包含权重、原始数据集及预训练缓存。数据和预训练权重可由 README 命令下载，我的训练所得权重随完整交付包提供。SHA256.json 用于核对源代码与报告日志文件，不包含模型权重。

本次通过网页分目录提交材料，远程提交历史与本地实验历史分别保留。后续修改我会先克隆远程仓库，再提交更新，不覆盖已有远程历史。
