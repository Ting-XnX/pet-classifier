# 宠物分类实验答辩提纲

王诗雯｜W125301282

## 我的实验思路

我使用 ImageNet 预训练 ResNet-18，将最后的全连接层从 1000 类改为 37 类。我选择全参数微调，让预训练特征适应宠物品种的细微差别。对照实验只将 Label Smoothing 系数从 0 改为 0.1，其余条件保持一致。

## 输入和网络维度

我的 batch 输入为 `[32,3,224,224]`。经过首层卷积变为 `[32,64,112,112]`，最大池化后为 `[32,64,56,56]`；四个残差阶段依次输出 `[32,64,56,56]`、`[32,128,28,28]`、`[32,256,14,14]` 和 `[32,512,7,7]`。全局平均池化得到 `[32,512,1,1]`，展平为 `[32,512]`，分类头输出 `[32,37]`。

## 我的数据划分

我先合并官方 trainval 和 test，再分层抽取 70% 作为训练集；剩余部分等分为验证和测试集。每类在三个集合中均有样本，索引没有交集。因为我使用自定义划分，所以不直接与官方测试划分下的论文数值比较。

验证和测试使用固定中心裁剪。我没有在评估时随机翻转或裁剪，避免同一图片在不同轮次得到不同输入。随机评估变换本身不等于标签泄漏，但会增加比较噪声；真正的数据泄漏还包括训练测试样本重叠、用测试成绩反复调参等。

## 损失和优化

交叉熵直接接收 logits，内部进行 log-softmax。我没有在模型最后添加 Softmax。Label Smoothing 将真实类的目标概率设为 `1-ε+ε/37`，其余类为 `ε/37`，其中 ε=0.1；它降低了模型对单一标签过度自信的倾向，但不保证每次实验都提高准确率。

PyTorch 默认累积梯度，所以我在每个 batch 反向传播前调用 `optimizer.zero_grad(set_to_none=True)`。训练中依次前向计算、计算损失、反向传播、更新参数。

## 我如何判断过拟合

如果训练损失持续下降而验证损失上升，说明拟合训练集的能力可能没有转化为泛化表现。我结合验证准确率共同判断；训练集有随机增强，而验证集没有，因此两者损失差距不能单独作为过拟合的严格证明。我使用基础增强、weight decay，并根据验证准确率保存最佳模型。我的脚本完整训练 10 轮，没有使用提前终止。

## 模型选择与测试

我只使用验证集选模型，选择 Top-1 最高的轮次；打平时保留较早的模型。测试集用于最终评估。Top-5 检查真实标签是否在最高的五个分数中；Macro-F1 先计算每一类的 F1，再对 37 类等权平均，防止只关注样本较多的类别。

## Grad-CAM 原理

我对最后一个残差阶段的特征图求预测类别分数的梯度，再对空间维度平均梯度，作为各通道权重。将通道特征加权求和并经过 ReLU，再放大至 224×224 与原图叠加。评估指标使用 `no_grad()`，但 Grad-CAM 必须保留梯度；两者不能共用无梯度上下文。

## 结论边界

我只运行一个随机种子，因此用“本次实验中提高或下降”描述结果，不使用“显著提升”。若继续研究，我会在相同协议下增加多个种子，并报告均值和标准差。热力图只能辅助理解，不能证明模型一定识别了某个解剖部位。
